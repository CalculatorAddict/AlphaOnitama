"""AlphaOnitama — an MCTS Onitama engine with an AlphaZero-style training loop.

Phase 1 (this package, stdlib-only core): game engine + pure MCTS with random
rollouts + a pygame GUI — a complete, playable agent.

Phase 2 (optional ``[ml]`` extra): a policy/value network and self-play loop
that replaces random rollouts. See the README for the two-phase overview.
"""

__version__ = "0.1.0"
